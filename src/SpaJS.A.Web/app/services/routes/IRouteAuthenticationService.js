var app;
(function (app) {
    var services;
    (function (services) {
        var routes;
        (function (routes) {
            'use strict';
        })(routes = services.routes || (services.routes = {}));
    })(services = app.services || (app.services = {}));
})(app || (app = {}));
